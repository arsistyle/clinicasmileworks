---
import { twMerge } from "tailwind-merge"
import type { NavItem } from "../../constants/types"

import { NAV_ITEMS } from "../../constants"

import NavColumnsChildrens from "./NavColumnsChildrens.astro"

const MenuWithoutHome = NAV_ITEMS.filter((item) => item.href !== "/")
---

<ul class="flex gap-10">
	{
		MenuWithoutHome?.map(({ href = "", label, children }: NavItem) => {
			const hasChildren = Array.isArray(children)

			return (
				<li class="group">
					<a href={href} class:list={twMerge("text-white transition-opacity hover:opacity-60")}>
						<span>{label}</span>
					</a>
					{hasChildren && <NavColumnsChildrens children={children} />}
				</li>
			)
		})
	}
</ul>
