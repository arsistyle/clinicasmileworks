---
import Main from "@/layouts/Main.astro"

const sections: any[] = await Astro.glob("../content/pages/home/*.astro")
---

<Main title={"Clínica Smile Works"}>
	{sections.map((component: any) => <component.default />)}
</Main>
